let &runtimepath.=','.escape(expand('<sfile>:p:h'), '\,')
" set rtp+=/Users/reecestevens/projects/vim-reviewer

lua << EOF

-- require("vim_reviewer");

EOF


" let g:python3_host_prog = $HOME . '/innolitics/10x/cli-github-pr-review/cli-review-venv/bin/python'
